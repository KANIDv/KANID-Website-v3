# Beispiel für die Umgebungsvariablen

# Resend API-Schlüssel (ersetzen Sie 're_example...' mit Ihrem tatsächlichen Schlüssel)
RESEND_API_KEY=re_example123456789

# HINWEIS: Diese Datei sollte in .env umbenannt und die tatsächlichen Werte eingetragen werden.
# Die .env-Datei sollte NICHT unter Versionskontrolle stehen (sie ist bereits in .gitignore). 